---
tags:
  - "#datr"
aliases: 
created:
  "{ date:YYYY-MM-DD }":
---
### **🚧 Проблема, которую решают mixin’ы:**

Dart **не поддерживает множественное наследование классов**. То есть класс может наследоваться **только от одного другого класса**.

Но иногда хочется **переиспользовать функциональность нескольких классов**, не нарушая принципов ООП. Вот тут и появляется mixin.

---

### **🧩 Что такое** 

### **mixin**

Mixin — это способ **внедрить повторно используемый код** в классы **без наследования**.  

Mixin — это обычный класс (или почти), но его **нельзя создавать напрямую**. Его **“подмешивают”** к другим классам с помощью ключевого слова with.
Пример:

```
mixin Logger {
  void log(String message) {
    print('LOG: $message');
  }
}

class MyService with Logger {
  void doSomething() {
    log('Doing something...');
  }
}

void main() {
  final service = MyService();
  service.doSomething(); // LOG: Doing something...
}
```

Здесь Logger — это миксин. Он добавляет метод log() в MyService. И при этом MyService **не наследует** Logger, он **“подмешивает”** его с помощью with.

---

### **⚙️ Разница между mixin и обычным классом**
С технической точки зрения:

- mixin не может иметь **конструкторов**;
    
- mixin **не может быть создан через new** (его нельзя инстанцировать напрямую);
    
- Миксины могут быть **ограничены** с помощью on, если нужно, чтобы они применялись только к определённым типам.
    

---

### **🔒 Ограничение с**  **on**

Иногда миксин должен использовать методы или поля, которые есть только в определённом типе. Тогда используют on:

```
class Animal {
  void eat() => print('Eating...');
}

mixin CanRun on Animal {
  void run() {
    print('Running...');
    eat(); // можем вызывать, потому что `on Animal`
  }
}

class Dog extends Animal with CanRun {}

void main() {
  final dog = Dog();
  dog.run(); // Running... Eating...
}
```

---

### **🧠** **with**  **— это способ применить mixin**

  

with — ключевое слово, которое используется в определении класса для подключения одного или нескольких миксинов:

```
class SomeClass with Mixin1, Mixin2, Mixin3 {}
```

Порядок миксинов важен: справа налево перекрываются методы (как в JS или Python).

---

### **🧪 Подводим итоги**

|**Что**|**Описание**|
|---|---|
|mixin|Специальный класс для переиспользования логики без наследования|
|with|Оператор для подключения миксинов к классу|
|Ограничение on|Позволяет использовать миксин только с определёнными типами|
|Нет конструкторов|В миксинах нельзя объявлять конструкторы|

