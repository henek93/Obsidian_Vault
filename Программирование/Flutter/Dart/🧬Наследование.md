---
tags:
  - "#datr"
aliases: 
created:
  "{ date:YYYY-MM-DD }":
---
Конечно. Dart — объектно-ориентированный язык, и **наследование** в нём работает похоже на другие ООП-языки, но с некоторыми особенностями. Давай разложим всё по полочкам, без приукрашиваний и с практической пользой.

---

## **🧬 Что такое наследование в Dart**

Наследование — это механизм **переиспользования кода**: ты создаёшь новый класс на основе уже существующего, **расширяя** или **переопределяя** его поведение.
В Dart это делается с помощью ключевого слова extends.

---

## **✅ Базовый пример**

```
class Animal {
  void breathe() => print('I can breathe');
}

class Dog extends Animal {
  void bark() => print('Woof!');
}

void main() {
  final dog = Dog();
  dog.breathe(); // унаследованный метод
  dog.bark();    // свой метод
}
```

- Dog наследует Animal, получает все его **открытые методы и поля**.
    
- Можно **добавлять новые методы**, или **переопределять** (override) существующие.
    

---

## **🔄 Переопределение методов**

```
class Animal {
  void sound() => print('Some animal sound');
}

class Cat extends Animal {
  @override
  void sound() => print('Meow');
}

void main() {
  final cat = Cat();
  cat.sound(); // Meow
}
```

@override — аннотация, показывающая, что ты **заменяешь метод родителя**.

---

## **👨‍👦 super: обращение к родительскому классу**

  

Ты можешь вызывать методы родителя через super.

```
class Animal {
  void eat() => print('Animal is eating');
}

class Dog extends Animal {
  @override
  void eat() {
    super.eat(); // вызов метода из родителя
    print('Dog is eating kibble');
  }
}
```

---

## **🏗️ Конструкторы и наследование**

  

В Dart **конструкторы не наследуются автоматически**, но можно явно вызвать родительский через super(...).

```
class Person {
  final String name;
  Person(this.name);
}

class Student extends Person {
  final int grade;

  Student(String name, this.grade) : super(name);
}
```

---

## **❗ Важные ограничения Dart**

| **Особенность**            | **Есть ли в Dart?**    |
| -------------------------- | ---------------------- |
| Множественное наследование | ❌ Нет                  |
| Интерфейсы                 | ✅ Да, через implements |
| Абстрактные классы         | ✅ Да                   |
| Миксины (with)             | ✅ Да                   |

---

## **📦 implements — альтернатива наследованию**

  

Интерфейсы в Dart — это просто **классы, реализованные через implements**, где ты **обязан переопределить все методы**.

```
class Logger {
  void log(String msg) {}
}

class FileLogger implements Logger {
  @override
  void log(String msg) {
    print('File: $msg');
  }
}
```

---

## **🔂 Разница между**  **extends** **,**  **implements****,  **with**

| **Ключевое слово** | **Что делает**      | **Особенности**                                            |
| ------------------ | ------------------- | ---------------------------------------------------------- |
| extends            | Наследует класс     | Можно переопределять, получать поведение и поля            |
| implements         | Реализует интерфейс | Надо вручную реализовать все методы                        |
| with               | Подмешивает mixin   | Позволяет переиспользовать поведение, **без наследования** |

---

## **🤔 Когда использовать что?**

- extends — когда нужен базовый класс с поведением и состоянием.
    
- implements — когда хочешь подписать класс под интерфейс (контракт).
    
- with — когда нужно подмешать общую функциональность в несколько классов.
    

---

## **🧪 Закрепим примером**

```
abstract class Vehicle {
  void move();
}

mixin Engine {
  void start() => print('Engine started');
}

class Car extends Vehicle with Engine {
  @override
  void move() => print('Car is moving');
}
```

---

Хочешь — могу сделать для этого всего наглядную схему или пример в стиле LinguaLink. Или мы можем перейти к практике: ты назовешь свой пример, а я помогу реализовать через extends, implements или with.
