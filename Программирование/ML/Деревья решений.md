# 🌳 Деревья решений

> **Мощный и интерпретируемый алгоритм машинного обучения для [[Классификация|классификации]] и регрессии**

**Деревья решений** (Decision Trees) — это алгоритм машинного обучения, который строит модель в виде дерева, где каждый внутренний узел представляет собой тест по [[Базовые термины|признаку]], каждая ветвь — результат теста, а каждый лист — [[Базовые термины|метку]] класса или числовое значение.

## 🎯 Принцип работы

Дерево решений работает по принципу **"разделяй и властвуй"** (divide and conquer), рекурсивно разбивая данные на меньшие подмножества.

### 🔄 Алгоритм построения:
1. **Начало:** Со всего набора данных (корень дерева)
2. **Выбор признака:** Найти лучший признак для разбиения
3. **Разбиение:** Разделить данные по этому признаку
4. **Рекурсия:** Повторить процесс для каждого подмножества
5. **Остановка:** Когда достигнут критерий остановки

![[Pasted image 20250917191838.png]]
*Пример структуры дерева решений: корень, внутренние узлы (тесты) и листья (классы)*

## 📉 Метрики качества разбиения

Ключевой вопрос: **как выбрать лучший признак для разбиения?** Ответ: с помощью метрик нечистоты (разнообразия).

### 1️⃣ Индекс Джини (Gini Index)

**Определение:** Мера нечистоты, показывающая вероятность неправильной классификации случайно выбранного объекта.

**Математическая формула:**
$$\text{Gini} = 1 - \sum_{i=1}^{c} p_i^2$$

**Где:**
- `c` — количество классов
- `p_i` — доля объектов класса i в узле

**Пример вычисления:**
Если в узле 50% объектов класса A и 50% класса B:
$$\text{Gini} = 1 - (0.5^2 + 0.5^2) = 1 - 0.5 = 0.5$$

**Значения:**
- **0** — узел полностью чистый (все объекты одного класса)
- **0.5** — максимальная нечистота для бинарной классификации

**Одним из показателей разнообразия набора данных является индекс Джини.**

### 2️⃣ Энтропия (Entropy)

**Определение:** Мера неопределенности или разнообразия в наборе данных. **Также хорошим примером разнообразия набора данных является энтропия.**

**Математическая формула:**
$$\text{Entropy} = -\sum_{i=1}^{c} p_i \log_2(p_i)$$

**Где:**
- `c` — количество классов
- `p_i` — доля объектов класса i в узле

**Интуитивное объяснение:** Чтобы понимать, что такое энтропия, можно представить, что у нас есть набор из 10 шариков, 8 из которых это красные, а 2 из которых это синие. И нам нужно подсчитать вероятность того, что мы сможем достать шарики по одному в той же последовательности. То есть точно так же 8 красных, а потом 2 синих. То есть вероятность этого очень маленькая, и чем она меньше, тем получается у нас разнообразнее набор данных.

![[Pasted image 20250919133036.png]]
*Пример вычисления энтропии для набора шариков*

**Почему используют логарифмы:** Из-за того что числа очень маленькие лучше оперировать логарифмами.

![[Pasted image 20250919133815.png]]
*Математическое обоснование использования логарифмов в энтропии*

**Значения энтропии:**
- **0** — полная определенность (все объекты одного класса)
- **1** — максимальная неопределенность для бинарной классификации
- **log₂(c)** — максимальная энтропия для c классов

**Пример вычисления:**
Для 50% класса A и 50% класса B:
$$\text{Entropy} = -(0.5 \times \log_2(0.5) + 0.5 \times \log_2(0.5)) = -(-0.5 - 0.5) = 1$$

### 3️⃣ Информационный выигрыш (Information Gain)

**Определение:** Уменьшение энтропии после разбиения по признаку.

**Формула:**
$$\text{Information Gain} = \text{Entropy(parent)} - \sum_{j} \frac{|S_j|}{|S|} \times \text{Entropy}(S_j)$$

**Где:**
- `S` — родительский узел
- `S_j` — дочерние узлы после разбиения
- `|S|` — количество объектов в узле

![[Pasted image 20250919134631.png]]
*Пример вычисления информационного выигрыша*

![[Pasted image 20250919134738.png]]
*Сравнение разных вариантов разбиения по информационному выигрышу*

## 🔍 Популярные алгоритмы деревьев решений

### 1️⃣ ID3 (Iterative Dichotomiser 3)
- **Критерий разбиения:** Информационный выигрыш (энтропия)
- **Особенности:** Работает только с категориальными признаками
- **Недостатки:** Склонен к переобучению, не работает с пропущенными значениями

### 2️⃣ C4.5
- **Критерий разбиения:** Gain Ratio (модифицированный информационный выигрыш)
- **Улучшения по сравнению с ID3:**
  - Работает с непрерывными признаками
  - Обрабатывает пропущенные значения
  - Выполняет обрезку дерева (pruning)

### 3️⃣ CART (Classification and Regression Trees)
- **Критерий разбиения:** Индекс Джини (для классификации), MSE (для регрессии)
- **Особенности:**
  - Строит только бинарные деревья
  - Работает с непрерывными и категориальными признаками
  - Поддерживает как классификацию, так и регрессию

![[Pasted image 20250919150220.png]]
*Сравнение различных алгоритмов построения деревьев решений*

## ⚙️ Критерии остановки

Дерево перестает расти когда:

### 🎯 Основные критерии:
1. **Чистота узла:** Все объекты в узле принадлежат одному классу
2. **Минимальное количество объектов:** В узле меньше заданного минимума объектов
3. **Максимальная глубина:** Достигнута максимальная глубина дерева
4. **Минимальный выигрыш:** Информационный выигрыш меньше порога
5. **Минимальное разбиение:** Нельзя разделить узел на части с достаточным количеством объектов

### 📊 Гиперпараметры:
- `max_depth` — максимальная глубина дерева
- `min_samples_split` — минимальное количество объектов для разбиения узла
- `min_samples_leaf` — минимальное количество объектов в листе
- `max_features` — максимальное количество признаков для рассмотрения при разбиении

## ⚖️ Преимущества и недостатки

### ✅ Преимущества:
- **Интерпретируемость:** Легко понять логику принятия решений
- **Не требует предобработки:** Работает с разными типами данных
- **Автоматический отбор признаков:** Использует только важные признаки
- **Нелинейные зависимости:** Может моделировать сложные взаимодействия
- **Работа с пропусками:** Многие реализации обрабатывают пропущенные значения
- **Быстрое предсказание:** O(log n) для предсказания

### ❌ Недостатки:
- **Переобучение:** Склонность к созданию слишком сложных деревьев
- **Нестабильность:** Небольшие изменения данных могут кардинально изменить дерево
- **Смещение:** Предпочтение признаков с большим количеством значений
- **Сложности с линейными зависимостями:** Плохо моделирует простые линейные отношения
- **Проблемы с несбалансированными данными:** Может игнорировать редкие классы

## 🛠️ Техники улучшения

### 1️⃣ Обрезка дерева (Pruning)

**Пре-обрезка (Pre-pruning):**
- Остановка роста дерева на основе критериев
- Используется во время построения дерева

**Пост-обрезка (Post-pruning):**
- Удаление ветвей после построения полного дерева
- Основана на валидационной выборке

### 2️⃣ Ансамблевые методы

**Random Forest:**
- Множество деревьев с случайным выбором признаков
- Усреднение предсказаний всех деревьев

**Gradient Boosting:**
- Последовательное построение деревьев
- Каждое новое дерево исправляет ошибки предыдущих

**AdaBoost:**
- Взвешенное голосование деревьев
- Увеличение веса неправильно классифицированных объектов

### 3️⃣ Обработка категориальных признаков

**Для упорядоченных категорий:**
- Можно использовать как числовые значения

**Для неупорядоченных категорий:**
- Бинарные разбиения для каждого значения
- One-hot encoding (не всегда оптимально)

## 🎯 Практические применения

### 🏥 Медицинская диагностика
**Пример:** Диагностика заболеваний по симптомам
```
Температура > 38°C?
├─ Да: Кашель есть?
│  ├─ Да: Грипп (вероятность 85%)
│  └─ Нет: Простуда (вероятность 70%)
└─ Нет: Здоров (вероятность 95%)
```

### 💰 Финансовый анализ
**Пример:** Одобрение кредита
```
Доход > 50,000?
├─ Да: Кредитная история хорошая?
│  ├─ Да: Одобрить кредит
│  └─ Нет: Дополнительная проверка
└─ Нет: Отказать в кредите
```

### 🛒 E-commerce
**Пример:** Рекомендация товаров
```
Возраст < 25?
├─ Да: Категория "Электроника"
│  ├─ Мужчина: Смартфоны
│  └─ Женщина: Наушники
└─ Нет: Категория "Дом и сад"
```

## 💻 Практическая реализация

### Python с Scikit-learn

```python
from sklearn.tree import DecisionTreeClassifier
from sklearn.model_selection import train_test_split
from sklearn.metrics import accuracy_score

# Создание и обучение модели
dt = DecisionTreeClassifier(
    criterion='gini',           # или 'entropy'
    max_depth=5,               # ограничение глубины
    min_samples_split=20,      # мин. объектов для разбиения
    min_samples_leaf=10,       # мин. объектов в листе
    random_state=42
)

# Обучение
dt.fit(X_train, y_train)

# Предсказание
y_pred = dt.predict(X_test)
accuracy = accuracy_score(y_test, y_pred)
print(f"Точность: {accuracy:.3f}")

# Важность признаков
feature_importance = dt.feature_importances_
print("Важность признаков:", feature_importance)
```

### Визуализация дерева

```python
from sklearn.tree import plot_tree
import matplotlib.pyplot as plt

plt.figure(figsize=(20,10))
plot_tree(dt, 
          feature_names=feature_names,
          class_names=class_names,
          filled=True,
          rounded=True,
          fontsize=12)
plt.show()
```

## 🔍 Сравнение Gini vs Entropy

| Аспект | Индекс Джини | Энтропия |
|--------|--------------|----------|
| **Скорость вычисления** | Быстрее | Медленнее (из-за логарифма) |
| **Диапазон значений** | [0, 0.5] для бинарной | [0, 1] для бинарной |
| **Предпочтения разбиения** | Склонен к созданию чистых узлов | Более сбалансированные разбиения |
| **Чувствительность** | Менее чувствителен к изменениям | Более чувствителен |
| **Использование** | CART алгоритм | ID3, C4.5 алгоритмы |

## 📈 Оценка качества модели

### Для классификации:
- **Accuracy:** Общая точность
- **Precision/Recall:** Для несбалансированных данных
- **F1-score:** Гармоническое среднее precision и recall
- **ROC-AUC:** Площадь под ROC-кривой

### Для регрессии:
- **MSE:** Среднеквадратичная ошибка
- **MAE:** Средняя абсолютная ошибка
- **R²:** Коэффициент детерминации

## 🔗 Связанные темы

- [[Базовые термины]] — Основные понятия ML
- [[Классификация]] — Задачи классификации
- [[Переобучение и недообучение]] — Проблемы сложных деревьев
- [[Достоверность(классификационная модель)]] — Метрики оценки качества
- [[🧭 ML Navigation]] — Вернуться к навигации

## 💡 Ключевые выводы

1. **Интерпретируемость:** Деревья решений легко объяснить и понять
2. **Контроль сложности:** Используйте ограничения глубины и размера узлов
3. **Ансамбли лучше:** Random Forest часто превосходит одиночные деревья
4. **Выбор метрики:** Gini быстрее, Entropy теоретически обоснованнее
5. **Предобработка не нужна:** Деревья работают с сырыми данными
6. **Осторожно с переобучением:** Всегда валидируйте на отдельной выборке

## 📖 Библиотеки для практики

- [[Библиотеки/Scikit-learn]] — DecisionTreeClassifier, DecisionTreeRegressor
- [[Библиотеки/NumPy]] — Математические вычисления
- [[Библиотеки/Pandas]] — Работа с данными
- [[Библиотеки/Matplotlib]] — Визуализация деревьев

#ml #ml/classification #ml/algorithms #ml/interpretable